# 킥온 관리자 개발 환경 전체 QA

- 실행일: 2026-09-08 (Asia/Seoul)
- 대상 브랜치: `Prod`
- 기준 커밋: `091b7f9` + 기존 미커밋 변경 및 이번 수정
- 개발 관리자/API: `http://localhost:3001`, `http://127.0.0.1:3002/api`
- 개발 Supabase project ref: `uvsmyftwwucrvoteajpi`
- 운영 관리자/Supabase: `https://admin.kickon.kr`, `smihjaucucffmsbktnmp`
- 공식 배포: `.github/workflows/deploy.yml`의 `Prod` push → S3/CloudFront 정적 배포 + Lambda
- 판정: **배포 준비 완료**. 기능 QA와 필수 자동 검사, 운영 DB 선행 객체 확인을 모두 통과했고 배포 후보 범위를 확정했다. 운영 배포 및 배포 후 확인 결과는 실행 뒤 이 문서에 추가한다.

## 요약

| 전체 기능 | 통과 | 부분 통과 | 차단 | 미실행 |
|---:|---:|---:|---:|---:|
| 29 | 29 | 0 | 0 | 0 |

- 실제 Chrome 조작, 관리자 API 응답, 개발 DB 행 및 감사 로그를 교차 확인했다.
- 공개 작은 화면은 375×812에서 확인했고 WCAG A/AA 자동 검사 결과 위반 0건이었다.
- 수정 후 자동 검사는 테스트 49/49, 타입, 린트, 웹 빌드, 관리자 API 빌드 모두 통과했다.
- 기존 업무 데이터와 이번 실행 이전의 QA 표식 데이터는 변경하지 않았다.

판정 기준:

- 통과: 요구한 정상·주요 오류 경로가 화면과 API/DB에서 일치
- 부분 통과: 핵심 경로는 확인했으나 전체 범위 중 필수 검증 일부가 없음
- 차단: 필요한 계정 또는 사용자 승인 등 현재 없는 외부 조건이 필요
- 미실행: 비용·외부 영향 때문에 실행하지 않은 기능

## 환경 및 안전 확인

| 항목 | 결과 | 증거 |
|---|---|---|
| 개발/운영 분리 | 통과 | 개발과 운영의 Supabase project ref가 다르고 대시보드 가입자 수도 각각 3명/2명으로 달랐다. 화면의 “개발” 표시에 의존하지 않고 연결 URL을 확인했다. |
| 개발 API | 통과 | API 3002, Next 3001. 허용 Origin의 무인증 요청은 401+CORS, 미허용 Origin은 403, OPTIONS는 204였다. |
| 운영 읽기 사전 확인 | 통과 | 운영 대시보드와 주요 메뉴를 읽기 중심으로 확인했다. 운영 DB에서 `202609080001`~`202609080008` 이력과 공지 생성·삭제 RPC, 일정 수정 RPC, 팬 활동 테이블의 실제 존재를 `SELECT`로 확인했으며 운영 쓰기는 수행하지 않았다. |
| 관리자 역할 | 통과 | 현재 서비스에 존재하는 관리자 역할은 `super_admin` 1개뿐이며 이것이 정상 설계임을 확인했다. 해당 실계정의 전체 메뉴 접근과 capability/API 경계를 확인했다. |
| 마이그레이션 이력 | 통과 | 관리자와 KickON 저장소에 공유 migration을 상호 미러링하고, 실제 DB 객체·권한·값을 검증한 `202609050013`, `202609070001~004`, `202609080007`을 개발 이력에 기록했다. 충돌한 팬 활동 migration은 `080008`로 이동했다. 이후 외부에서 개발 DB에 `080008`이 동시 적용된 것을 감지해 실제 테이블 2개·함수 7개·트리거 7개·권한·레벨 기준을 확인했다. 양쪽 목록과 dry-run은 최종 일치한다. |
| 외부 동기화 비밀값 | 통과 | 로컬 `.env.development.local`, 개발 Supabase Edge Function secret, DB Vault의 `kickon_football_sync_secret`을 원문 노출 없이 지문으로 비교해 모두 일치함을 확인했다. 관리자 API와 Next 개발 서버를 재시작했고 웹 200, 무인증 API의 기대 응답 401을 확인했다. |

## 기능별 결과

| # | 영역/경로 | 필요 권한 | 정상 및 오류 시나리오 | 결과와 증거 |
|---:|---|---|---|---|
| 1 | 로그인·세션 `/login` | 관리자 | 정상·실패 로그인, 로그아웃, 만료 세션, 무인증 직접 URL | **통과** — 무인증 `/users`가 `/login?next=...`로 이동, 가짜 계정 오류와 입력 유지, 가짜 만료 토큰 재로그인 유도, 실제 로그아웃 후 로그인 화면 이동을 확인했다. 사용자가 비밀번호를 직접 입력해 새 세션으로 정상 로그인했고 대시보드와 데이터 관리 화면 재접근까지 통과했다. |
| 2 | 역할별 UI/API 권한 | 역할별 capability | 실재 역할의 메뉴·버튼 및 직접 API 우회 | **통과** — 정상 설계상 실재 역할은 super_admin뿐이다. 해당 세션의 전체 메뉴 접근, 서버 capability 검사, bearer 누락 및 허용되지 않은 Origin 차단을 확인했다. |
| 3 | 대시보드 `/` | `dashboard.read` | 요약 수치·작업 링크·빈/실패 상태 | **통과** — 개발/운영 로드와 환경 분리를 확인했다. 인증된 375px Chrome 창에서 모바일 메뉴·지표·동기화 카드·운영 알림을 확인했고 가로 넘침과 오류 오버레이가 없었다. |
| 4 | 공지 목록·탭·상세 `/community` | `moderation.read` | 전체/팀별 5:5 탭, 팀 필터·엠블럼, 빈 목록, 행/Enter 상세 | **통과** — 전체는 제목·등록 시각, 팀별은 대상 팀 포함. 행과 Enter로 상세 모달을 열고 Escape로 닫았다. |
| 5 | 전체 공지 생성 | `moderation.write` | 팀 없이 등록→목록→상세→DB·감사, 필수값 | **통과** — 팀 선택 없이 실제 등록되며 “유효한 대상 팀” 오류가 발생하지 않는다. 성공 알림은 3초 뒤 사라졌다. |
| 6 | 팀별 공지 생성 | `moderation.write` | K리그1 팀 선택·드롭다운·엠블럼, 팀 미선택 | **통과** — 김천 상무 선택과 DB 대상 팀을 확인했다. 드롭다운 항목 높이·너비와 모달 고정 높이를 확인했다. |
| 7 | 공지 삭제 | `moderation.write` | 확인 모달, 삭제 중 문구, 목록/DB/감사 | **통과** — 실제 UI 삭제 후 DB 제거를 확인했고 버튼은 “저장 중”이 아닌 “삭제 중”을 표시한다. 이번 실행 공지는 정리 완료. |
| 8 | 사용자 목록 `/users` | `users.read` | 검색·상태·팀 필터, 페이지/빈 결과, 직접 URL | **통과** — 검색 URL·새로고침 유지, 정상+김천 필터, 잘못된 UUID 오류를 확인했다. 검색 버튼 돋보기 제거와 모든 드롭다운 트리거 너비 일치를 확인했다. |
| 9 | 사용자 경고·정지·해제 | `users.moderate` | 오류 입력 유지, 종료 시각, DB 상태·행동·감사 | **통과** — 누락 오류 뒤 선택/사유 유지, 실제 SUSPEND→UNSUSPEND, DB·감사 확인 후 원상 복구 및 QA 행 정리 완료. |
| 10 | 문의 목록·상세 `/inquiries` | `support.read` | 검색·필터·직접 URL·날짜·빈 결과 | **통과** — ANDROID 검색과 상세, 빈 결과를 확인했다. 날짜를 `9월 6일 오후 10시 13분` 형식으로 통일했다. |
| 11 | 문의 답변·상태 | `support.write` | 답변 저장, ANSWERED 답변 누락, DB·감사·원복 | **통과** — 누락을 차단하고 실제 저장·감사 후 원래 상태로 복구했다. |
| 12 | 신고 처리 `/moderation/detail` | `moderation.read/write` | OPEN→RESOLVED, 메모 누락, 숨김→복구, DB·감사 | **통과** — 전용 신고 fixture로 메모 누락 오류 뒤 입력 유지, 처리 완료, 원문 HIDDEN/RESTORE와 감사 로그를 확인했다. fixture 전부 정리 완료. |
| 13 | 선수 목록 `/squads` | `data.read` | 검색·페이지·빈 결과·잘못된 상세 | **통과** — 김지훈 검색, 31~60/460 페이지, 빈 결과와 직접 상세 오류를 확인했다. |
| 14 | 선수 생성 | `data.write` | 수동 선수 생성→목록·상세·DB·감사, 필수/범위 | **통과** — `manual_0e049a90-3831-4ef3-96f7-8b665debaec5` 생성과 상세/DB를 확인했다. |
| 15 | 선수 수정 | `data.write` | 취소 미저장, 99→98·체중 70, 새로고침·DB·감사 | **통과** — 취소 후 99 유지, 저장 후 상세·DB와 필드별 감사 기록 일치. |
| 16 | 선수 삭제 | `data.write` | 사유 누락, 실제 삭제→목록 제외·DB 잠금, 직접 URL | **통과** — 사용자 승인 후 UI에서 실제 삭제했다. 목록 461→460명, 검색 0건, 직접 상세 오류, DB `in_squad=false`·동기화 차단 override·`PLAYER_DELETED` 감사를 확인한 뒤 전용 데이터를 정리했다. |
| 17 | 일정 목록 `/schedules` | `data.read` | 검색·상태·팀·페이지·빈 결과 | **통과** — 김천 33건/2페이지, 예정 6건과 빈 결과를 확인했다. |
| 18 | 일정 수정·보호 해제 | `data.write` | 19:30→19:31→원복, 실제 변경 필드만 잠금, UI 해제 | **통과** — `sportmonks-19648065`에서 `kickoff_at`만 override됨을 DB로 확인하고, 원본 복귀 시 자동 해제 및 별도 UI 보호 해제를 실제 수행했다. 최종 19:30·활성 override 0으로 복구. |
| 19 | 팀 목록·상세 `/standings` | `data.read` | 12팀, 김천 상세, 잘못된 teamId | **통과** — 목록·상세·오류 화면을 확인했다. |
| 20 | 팀 선수 필터·정렬 | `data.read` | 골키퍼·나이 정렬·빈 조건 | **통과** — 골키퍼 6명, 나이 오름차순과 빈 상태를 확인했다. |
| 21 | 팀 기록 수정·보호 해제 | `data.write` | 클린시트 6→7, override, UI 해제, DB 원복 | **통과** — 활성 보호값을 상세에 연결해 브라우저에서 해제했고 최종 클린시트 6·활성 override 0으로 정리했다. |
| 22 | 구단 선수단 동기화 | `sync.run` | 김천 단건 실행, 상태·결과·수동값 보존 | **통과** — `succeeded/cached` 완료와 QA 수동 선수 보존을 확인했다. 실행 행은 정리 완료. |
| 23 | 라인업·지표·실시간·전체·사후·과거 동기화 | `sync.run` | 완료·실패·쿼터·중복 실행 | **통과** — 실시간은 HTTP 200 `already-running`과 쿨다운 후 HTTP 202 `accepted`, 전체·종료 경기·과거 시즌은 실제 UI 실행, 종료된 김천 경기 라인업은 `cached`를 확인했다. 전체·종료 경기·라인업·과거 시즌 `sync_runs` 4건이 모두 `succeeded`, 완료 시각 존재, 오류 NULL임을 DB로 교차 검증했다. 2024~2026 시즌은 모두 READY였다. 표시 한도를 일시 조정해 저쿼터 경고·필수 확인·미확인 제출 차단과 DB 실행 행 미생성까지 확인한 뒤 2,000회로 원복했다. |
| 24 | 사용량 `/usage`·API | `system.read` | DB/스토리지/provider 지표, API 실패/CORS | **통과** — DB 약 19MB/500MB, API 분리와 CORS 수정 후 정상 표시 및 실패 요청 부재를 확인했다. |
| 25 | 감사 `/audit/detail` | `audit.read` | 목록·상세·관련 링크·없는 ID | **통과** — 선수/제재/일정 before·after를 확인했고 없는 `auditId=999999999`은 명확한 오류와 목록 복귀를 표시했다. |
| 26 | 테마 | 로그인 사용자 | 다크↔라이트, 새로고침 유지 | **통과** — 실제 전환과 새로고침 후 light 유지, 마지막에 dark로 복구했다. |
| 27 | 반응형·키보드 | 화면/모달 | 375×812, 가림·넘침, Enter/Escape/포커스 | **통과** — 공개 로그인·개인정보·계정삭제와 인증된 대시보드·공지사항을 375px Chrome에서 확인해 가로 넘침 0, 오류 오버레이 0이었다. 모바일 메뉴로 공지 화면을 이동했고 공지 행 Enter·모달 Escape·저쿼터 필수 체크 포커스를 확인했다. |
| 28 | 법적 고지 `/privacy`, `/account-deletion` | 공개 | 직접 URL·이미지·작은 화면 | **통과** — 두 경로 375px에서 넘침·깨진 이미지 0. |
| 29 | 관리자 API `/api/admin/*` | bearer + capability | 인증 요청, 무인증·bad origin·OPTIONS | **통과** — CORS/무인증 경계와 super_admin 인증 요청을 확인했다. 정상 재로그인으로 새로 발급된 세션에서 `/data-management/`가 200으로 로드되고 사용량·동기화 상태·관리자 로그 18건이 표시됐으며 브라우저 오류 로그가 없었다. |

## 발견한 문제와 수정

| ID | 문제/원인 | 수정 | 실제 재검증 |
|---|---|---|
| QA-01 | 전체 공지가 legacy `posts.team_id NOT NULL` 때문에 팀을 요구 | LEAGUE 공지는 운영자 입력 없이 RPC 내부에서 저장팀을 결정 | 팀 없이 등록→DB·감사→상세 확인 |
| QA-02 | 전체 공지 상세에 내부 저장팀/작성자가 노출 | 팀 공지만 팀명 표시하고 작성자 제거 | 상세 모달 확인 |
| QA-03 | 전체/팀별 작성 탭에 따라 모달 높이가 변함 | 제목을 위로 올리고 본문 영역을 늘려 두 모달 높이 고정 | 두 탭에서 동일 높이 확인 |
| QA-04 | 날짜가 `9.8.`, `오후 16시` 등으로 혼재 | 공통 한국어 12시간제 formatter 사용 | `9월 8일 오후 1시 36분`, 선수 상세 `오후 4시` 확인 |
| QA-05 | Next와 로컬 API가 3001 충돌 | API 기본 3002, 3001 웹 Origin 허용 | 사용량 API·CORS·로그 확인 |
| QA-06 | 사용자 정지 실패 뒤 select가 경고로 초기화 | controlled 선택, 정지 외 날짜 비활성화, 오류 reset 방지, prop 전환 시 key 재초기화 | 오류 뒤 날짜만 보완해 재제출 후 SUSPEND 확인 |
| QA-07 | 일정 RPC가 안 바뀐 위치까지 5개 override | SQL에서 원본과 실제 달라진 필드만 upsert하고 원본 복귀 시 release | 시간만 변경 시 override 1개, 원복 시 0개 |
| QA-08 | 일정·팀 활성 override 해제 UI가 없음 | 실제 override 조회 및 공용 보호 해제 UI 연결 | 일정과 팀 모두 브라우저 해제→DB `blocks_sync=false` |
| QA-09 | 전체 공지로 바꾼 뒤 이전 팀 오류가 남음 | scope 변경 시 teamId 제거, 현재 입력과 무관한 action 오류 숨김 | 전체 공지 팀 없이 등록 |
| QA-10 | 신고 폼에 문의 상태 용어가 섞이고 오류 뒤 입력 소실 | 신고 전용 용어, reset 방지, 성공 후만 최신 prop으로 초기화 | 메모 누락 오류 뒤 상태/사유 유지 후 성공 |
| QA-11 | 사용자 팀 엠블럼의 Next Image 비율 경고 | 40px relative wrapper와 `fill/object-contain`으로 비율 보존 | `/users` 재진입 후 서버 브라우저 경고 0 |
| QA-12 | 강제 외부 동기화가 HTTP 401 | Edge Function·DB Vault·로컬 값을 원문 없이 지문 비교해 로컬 값만 불일치함을 확인하고, Vault 원문으로 로컬 설정을 정합화 | 세 값의 지문 일치, 관리자 API·Next 재시작, 제한된 실시간 동기화 HTTP 202 `accepted` 및 후속 성공 시각 확인. 원격 secret은 회전하지 않아 기존 크론 영향 없음 |
| QA-13 | 두 저장소의 migration 누락과 `202609080005` 번호 충돌 | 적용 파일 상호 미러링, 팬 활동 migration을 `202609080008`로 이동, 수동 적용된 6개 이력을 객체 검증 후 repair | 양쪽 `migration list --linked` 및 `db push --dry-run` 완전 일치. 외부 동시 적용된 `080008`도 DB 객체·권한·레벨 계산 검증 |
| QA-14 | 전체 동기화는 실제로 성공하지만 브라우저 공통 API 제한이 12초라 먼저 실패 표시 | 일반 조회는 12초를 유지하고 동기화 작업만 58초 제한을 사용하도록 `callAdminAction`에 전용 제한시간 전달 | 재현 실행은 DB에서 50.8초 뒤 `succeeded`였고 UI만 12초 오류. 수정 후 종료 경기 보강은 약 4.7초 뒤 UI 성공, 새로고침 상태 정상·오류 없음, DB `succeeded` 확인. 브라우저 번들 테스트 추가 |
| QA-15 | 개발용 `127.0.0.1:3001` fallback 문자열이 운영 정적 청크에 남아 CI 산출물 검사를 실패시킴 | 개발 기본 fallback을 동등한 `localhost:3001/api`로 변경하고 무설정 fallback 회귀 테스트 추가 | 테스트 50/50 통과. CI와 동일한 공개 placeholder 환경으로 다시 빌드해 정적 24개 경로 생성, 금지 로컬 주소 0건·서버 비밀값 0건 확인 |

## 자동 검사

| 검사 | 결과 |
|---|---|
| `npm test` | **50/50 통과**, fail/skip 0 |
| `npm run typecheck` | 통과 |
| `npm run lint` | 통과 |
| `npm run build:static` | 통과 — CI와 동일한 공개 설정, Next.js 16.3.3, 정적 24개 경로 생성, `127.0.0.1:3001`·서버 비밀값 0건 |
| `npm run build:admin-api` | 통과 — `dist/admin-api/index.js` 854.1kb |
| `git diff --check` | 통과 |
| KickON `__tests__/fanLevel.test.ts` | **24/24 통과** (`--no-watchman`; 기본 Watchman은 샌드박스 상태 파일 권한으로 실행 불가) |

React 최종 검토에서는 effect 내 동기 setState를 제거하고 prop 상태 변경 시 폼을 key로 재마운트해 렌더 연쇄와 오류 입력 소실을 함께 방지했다. 일정/팀 데이터 요청은 서로 독립적인 호출을 병렬 실행하며, 권한 검사는 서버 action/RPC에서도 유지된다.

## 테스트 데이터 정리

| 데이터 | 식별자 | 상태 |
|---|---|---|
| 이번 실행 공지 | `QA 전체 공지 검증`, `[QA-20260908-172000] ...` 등 | 게시글·감사 로그 삭제 완료 |
| 신고 fixture | post `9a000000-0000-4000-8000-000000000908`, report `9b000000-0000-4000-8000-000000000908` | post/report/moderation action/audit 전부 삭제 완료 |
| 일정 | `sportmonks-19648065` | 원본 19:30, 좌표 NULL, 반경 300, 활성 override 0; QA override/audit/sync run 삭제 완료 |
| 팀 기록 | `gimcheon.clean_sheets` | 원본 6, 활성 override 0; QA override/audit 삭제 완료 |
| 사용자 제재 | `89acfbb1-9962-4d6e-bf4c-7784d0648066` | 원래 ACTIVE; 이번 실행 action/audit/state 행 삭제 완료 |
| 단건 동기화 | `c5b76c89-ca84-411e-89d5-5d47258b1b09` | id와 사유 일치 조건으로 삭제 완료 |
| 전체·종료 경기·라인업·과거 시즌 동기화 | 사유 `[QA-20260908]%` 4건 | 모두 `succeeded`·오류 NULL 확인 후 사유 접두사 일치 조건으로 삭제, 잔여 0건 |
| 수동 선수 | `manual_0e049a90-3831-4ef3-96f7-8b665debaec5` | UI 삭제·목록 제외·DB 잠금·감사 확인 후 선수/override/audit 삭제 완료 |
| 기존 QA 데이터 | `QA_RC_0906_*`, 이전 `[QA] 개발 선수 수정 검증` | 이번 실행 소유가 아니므로 보존 |

최종 잔여 스캔: 이번 실행 표식 `[QA-20260908`과 전용 식별자를 기준으로 `posts`, `team_players`, `manual_overrides`, `sync_runs`, `user_moderation_actions`, `content_moderation_actions`, `content_reports`, `user_data_reports`, `admin_audit_logs` 모두 0건.

## 화면 증거

- [로그인 375×812 화면](./2026-09-08-login-mobile.png): 가로 넘침 없음
- WCAG A/AA 자동 검사: violation 0, incomplete 0, pass 23
- 브라우저 DOM과 개발 서버 로그로 공지/신고/사용자/선수/일정/팀/테마 상태 및 이미지 경고 부재를 확인했다. 인증정보와 개인정보는 캡처에 포함하지 않았다.

## 배포 전 최종 판정

- 관리자 기능·API·DB QA 29/29 통과.
- 배포 후보에는 검증된 관리자 코드·테스트·문서·공유 migration만 포함했다.
- 코드에서 참조되지 않은 사용자 소유 `public/promo` 이미지 2개는 커밋과 배포 대상에서 제외했다.
- 운영 DB에는 필요한 최신 migration과 핵심 RPC·테이블이 이미 있어 이번 배포에서 DB 쓰기 작업은 필요하지 않다.

참고: 개발 DB schema dump는 Docker/Podman이 없어 생성하지 못했고 Supabase 관리 백업/PITR도 활성화되어 있지 않았다. 이력 repair는 실제 스키마를 변경하지 않으며 각 버전을 `--status reverted`로 되돌릴 수 있다. `080008`은 이 작업에서 push하지 않았지만 외부에서 동시에 적용됐고, 감지 즉시 읽기 전용 검증을 수행했다.

운영 DB에는 최신 공유 이력 `202609080001`~`202609080008`과 이번 관리자 코드가 사용하는 핵심 객체가 이미 존재하므로 이번 코드 배포를 위해 추가 migration을 실행할 필요가 없다.

운영 커밋·배포 식별자·완료 시각과 배포 후 읽기 검증 결과는 배포 완료 뒤 기록한다.

## 배포 전후 확인 및 롤백

배포 재개 전:

1. 배포할 커밋을 확정하고 그 동일 커밋에서 전체 자동 검사를 재실행한다.

배포 후에는 `https://admin.kickon.kr`의 배포 버전, 로그인, 주요 조회 API, 이미지, 운영 Supabase/Lambda 연결, CloudFront 자산, 신규 콘솔/서버 오류를 읽기 중심으로 확인한다. 운영 파괴적 테스트는 하지 않는다.

롤백:

- 정적 프런트는 직전 정상 S3 산출물과 CloudFront 배포로 복귀한다.
- Lambda는 직전 정상 코드/버전으로 복귀한다.
- DB 함수/스키마는 코드 롤백과 별개다. 운영 적용 전 함수 정의와 schema dump를 보관하고, 역방향 변경이 안전하지 않으면 검증된 forward-fix로 복구한다.
